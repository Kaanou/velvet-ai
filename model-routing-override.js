/* Velvet AI: no hard override — models come from advanced settings in ai.js */
(() => {
  // Intentionally minimal: ai.js already sends the selected model.
  // Kept for cache-bust compatibility.
})();
